import React from 'react';
import ReactDOM from 'react-dom';

export default function App() {
  return <div>Initial app component for begin coding.</div>;
}

ReactDOM.render(<App />, document.getElementById('app'));
